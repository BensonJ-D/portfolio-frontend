import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    background: {
      default: '#222222'
    },
    primary: {
      main: '#ffffff'
    }
  }
});

export default Theme;
